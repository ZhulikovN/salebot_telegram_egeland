# Общий секрет между основным бэкендом и relay — генерировать длинной случайной строкой,
# например: python3 -c "import secrets; print(secrets.token_hex(32))"
RELAY_SHARED_SECRET=change_me_to_a_long_random_string
